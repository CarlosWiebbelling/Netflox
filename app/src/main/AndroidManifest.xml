<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.series">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.AboutMe"></activity>
        <activity android:name=".screens.Register" />
        <activity android:name=".screens.Playlist" />
        <activity
            android:name=".screens.MenuSidebar"
            android:label="@string/title_activity_menu_sidebar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".screens.Favorites" />
        <activity
            android:name=".menu_sidebar"
            android:label="@string/title_activity_menu_sidebar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".screens.Login" />
        <activity android:name=".screens.Detail" />
        <activity android:name=".screens.Browser" />
        <activity android:name=".animation.SplashArt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>